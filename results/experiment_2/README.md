# Experiment 2 Results

Formal laptop runs are written to `results/experiment_2/runs/<invocation_id>/`.

The Experiment 2 runner intentionally writes compact scientific evidence only,
so the complete invocation directory can be committed to GitHub after the run.

Do not delete or overwrite unfavorable runs. Use a new invocation ID for any
later experiment version.
